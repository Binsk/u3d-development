/// @about
/// Animation poses contain a list of Bone structures that represent a single
/// pose at a set point in time. Animations contain numerous poses that are
/// interpolated between each-other to make a model move. Poses are NOT guaranteed
/// to contain a morph for every bone in the model and will only contain bones
/// that are affected.

function AnimationPose() : U3DObject() constructor {
	
}